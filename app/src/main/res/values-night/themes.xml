<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Frequency_app" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:windowBackground">@color/pastel_background_dark</item>
        <item name="fontFamily">@font/poppins</item>

        <item name="colorAccent">@color/pastel_accent_2</item>
        <item name="android:tint">?attr/colorAccent</item>

    </style>

    <style name="CustomDialog" parent="@style/Theme.AppCompat.Light.Dialog">
        <item name="android:windowNoTitle">false</item>
    </style>
</resources>